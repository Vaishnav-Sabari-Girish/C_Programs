// Strings are an array of characters and are teminated with a \0 in the end automatically.

#include <stdio.h>

int main() {
  char str[] = "This is a string";

  printf("String : %s\n", str);
  return 0;
}
